# portfolio

React portfolio starter built with Vite.

## Run locally

1. Install dependencies with `npm install`.
2. Start the dev server with `npm run dev`.
3. Build for production with `npm run build`.

## What is included

- A responsive single-page portfolio layout.
- Project, skills, and contact sections.
- Modern dark visual style with polished cards and buttons.
